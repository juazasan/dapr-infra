apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
  namespace: dapr-redis
spec:
  type: state.redis
  metadata:
  - name: redisHost
    value: redis-master.dapr-redis.svc.cluster.local:6379
  - name: redisPassword
    secretKeyRef:
      name: redis
      key: redis-password